{
  "name": "circleup-services",
  "private": true,
  "workspaces": [
    "verification",
    "sos",
    "genie",
    "ads",
    "compliance"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "typescript": "^5.6.0",
    "ts-node": "^10.9.2",
    "@types/node": "^22.10.0"
  }
}
