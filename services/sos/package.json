{
  "name": "@circleup/sos",
  "private": true,
  "version": "0.0.1",
  "description": "SOS Dispatch Service — SOS fan-out to police/helpline/contacts/neighbours (Group E)",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}
